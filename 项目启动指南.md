# 🚀 Pet Care Ecosystem 项目启动指南

## 📋 环境要求

### 必需软件
- **Java 21** - 推荐使用 OpenJDK 或 Oracle JDK
- **Node.js 18+** - 前端开发环境
- **MySQL 8.0+** - 数据库服务
- **Maven 3.8+** - Java项目构建工具

### 开发工具推荐
- **IntelliJ IDEA** - Java开发
- **VSCode** - 前端开发
- **DBeaver** - 数据库管理

## 🗄️ 数据库准备

### 1. 创建数据库
```sql
CREATE DATABASE petcare_ecosystem CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2. 创建用户（可选）
```sql
CREATE USER 'petcare_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON petcare_ecosystem.* TO 'petcare_user'@'localhost';
FLUSH PRIVILEGES;
```

## 🔧 配置文件设置

### 1. 全局基础服务地址（不提交到仓库）

在项目根目录创建或修改 `program-config.yml`（**已被 .gitignore 忽略，不会上传到远程仓库**）：

```yaml
nacos:
  host: 8.136.202.119
  port: 8848

mysql:
  host: 8.136.202.119
  port: 3306

redis:
  host: 8.136.202.119
  port: 6379
```

所有后端微服务的 `application.yml` / `application-dev.yml` 已经统一写成：

```yaml
spring:
  config:
    import: optional:file:../program-config.yml
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.host:localhost}:${nacos.port:8848}
  datasource:
    url: jdbc:mysql://${mysql.host:localhost}:${mysql.port:3306}/${mysql.database:pet_system}?...
```

以后如果更换服务器或数据库名，只需要修改 `program-config.yml` 一处即可。

### 2. 数据库连接配置

根据你当前的数据库部署，确认以下配置文件中的连接信息正确（IP/库名/账号）：

- `petcare-backend/src/main/resources/application-dev.yml`
- `media-backend/src/main/resources/application.yml`
- `community-backend/src/main/resources/application.yml`

（这些文件目前指向统一的远程数据库 `pet_system`，如需本地开发，可以改成 `localhost` + 本地库名。）

## 🚀 启动步骤

### 方法一：按顺序启动（推荐新手）

> 说明：所有微服务（除了网关）都使用 `server.port: 0` 随机端口，并通过 **Nacos + 网关** 进行调用，无需再写死端口。

#### 1. 启动 PetCare 后端服务（随机端口）
```bash
cd petcare-backend
.\mvnw.cmd clean compile spring-boot:run
```

看到类似输出表示成功（端口由 Nacos 管理）：
```text
Started PetcareBackendApplication in X.XXX seconds
```

#### 2. 启动 Media 后端服务（随机端口）
```bash
# 新开一个终端窗口
cd media-backend
.\mvnw.cmd clean compile spring-boot:run
```

#### 3. 启动 Community 后端服务（随机端口）
```bash
# 新开一个终端窗口
cd community-backend
.\mvnw.cmd clean compile spring-boot:run
```

#### 4. 启动 LLM 后端服务（随机端口，可选）
```bash
# 新开一个终端窗口
cd llm-backend
.\mvnw.cmd clean compile spring-boot:run
```

#### 5. 启动 Statistics 后端服务（随机端口，可选）
```bash
# 新开一个终端窗口
cd statistics-backend
.\mvnw.cmd clean compile spring-boot:run
```

#### 6. 启动网关服务（固定端口 9000，前端所有请求都打到这里）
```bash
# 新开一个终端窗口
cd petcare-gateway
.\mvnw.cmd clean compile spring-boot:run
```

#### 7. 启动前端服务（端口5173，开发模式）
```bash
# 新开一个终端窗口
cd frontend
npm install  # 首次运行需要安装依赖
npm run dev
```

**验证启动成功：**
```
VITE v5.4.21 ready in XXX ms
➜ Local: http://localhost:5173/
```

### 方法二：使用启动脚本（推荐熟练用户）

#### Windows系统
**文件**: `start-all.bat` (已提供)
```bash
# 双击运行或在命令行执行
start-all.bat
```

#### Linux/Mac系统
**文件**: `start-all.sh` (已提供)
```bash
chmod +x start-all.sh
./start-all.sh
```

## 🔍 验证服务状态

### 检查端口占用
```bash
# Windows
netstat -ano | findstr ":808"

# Linux/Mac
lsof -i :8081,8082,8084,5173
```

### API测试
使用浏览器或Postman测试以下API：

#### 通过网关访问 API（示例）

- PetCare 相关：`http://localhost:9000/api/pets/...`
- Media 相关：`http://localhost:9000/api/media/...`
- Community 相关：`http://localhost:9000/api/v1/moments/user/76`

#### 前端页面
```bash
http://localhost:5173
```

## 🐛 常见问题解决

### 1. 端口被占用
**错误**: `Port 8082 was already in use`

**解决方法**:
```bash
# Windows: 查找并杀掉占用端口的进程
netstat -ano | findstr :8082
taskkill /PID [进程ID] /F

# Linux/Mac:
lsof -ti:8082 | xargs kill -9
```

### 2. 数据库连接失败
**错误**: `Could not create connection to database server`

**解决方法**:
- 检查MySQL服务是否启动
- 验证数据库用户名密码
- 确认数据库名称正确

### 3. Maven编译失败
**错误**: `Failed to execute goal org.apache.maven.plugins`

**解决方法**:
```bash
# 清理Maven缓存
mvn clean

# 重新编译
mvn compile

# 如果还有问题，删除.m2文件夹重试
```

### 4. Node.js依赖安装失败
**错误**: `npm ERR! code ERESOLVE`

**解决方法**:
```bash
# 清理npm缓存
npm cache clean --force

# 删除node_modules和package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 5. Java版本不兼容
**错误**: `Unsupported class file major version`

**解决方法**:
- 确保使用Java 21
- 检查JAVA_HOME环境变量
```bash
java -version
echo $JAVA_HOME
```

## 📞 技术支持

如果遇到其他问题，请：

1. **检查日志**: 查看详细的错误信息
2. **查看文档**: 参考 `后端改动与问题记录.md`
3. **联系团队**: 在项目群中询问具体错误信息

### 常用调试命令
```bash
# 查看Java进程
jps -l

# 查看系统资源
htop  # Linux/Mac
tasklist  # Windows

# 测试数据库连接
mysql -h localhost -u username -p petcare_ecosystem
```

---

**祝您开发愉快！** 🎉