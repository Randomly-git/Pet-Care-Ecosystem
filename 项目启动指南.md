# 🚀 Pet Care Ecosystem 项目启动指南

## 📋 环境要求

### 必需软件
- **Java 21** - 推荐使用 OpenJDK 或 Oracle JDK
- **Node.js 18+** - 前端开发环境
- **MySQL 8.0+** - 数据库服务
- **Maven 3.8+** - Java项目构建工具

### 开发工具推荐
- **IntelliJ IDEA** - Java开发
- **VSCode** - 前端开发
- **DBeaver** - 数据库管理

## 🗄️ 数据库准备

### 1. 创建数据库
```sql
CREATE DATABASE petcare_ecosystem CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2. 创建用户（可选）
```sql
CREATE USER 'petcare_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON petcare_ecosystem.* TO 'petcare_user'@'localhost';
FLUSH PRIVILEGES;
```

## 🔧 配置文件设置

### 数据库连接配置
确保以下配置文件中的数据库连接信息正确：

#### PetCare后端
**文件**: `petcare-backend/src/main/resources/application.yml`
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/petcare_ecosystem?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: your_username
    password: your_password
```

#### Media后端
**文件**: `media-backend/src/main/resources/application.yml`
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/petcare_ecosystem?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: your_username
    password: your_password
```

#### Community后端
**文件**: `community-backend/src/main/resources/application.yml`
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/petcare_ecosystem?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: your_username
    password: your_password
```

## 🚀 启动步骤

### 方法一：按顺序启动（推荐新手）

#### 1. 启动PetCare后端服务（端口8082）
```bash
cd petcare-backend
./mvnw clean compile spring-boot:run -Dspring-boot.run.arguments="--server.port=8082"
```

**Windows用户如果无法使用./mvnw：**
```bash
cd petcare-backend
mvn clean compile spring-boot:run -Dspring-boot.run.arguments="--server.port=8082"
```

**验证启动成功：**
看到以下输出表示成功：
```
Started PetCareEcosystemBackendApplication in X.XXX seconds
Tomcat started on port 8082
```

#### 2. 启动Media后端服务（端口8081）
```bash
# 新开一个终端窗口
cd media-backend
./mvnw clean compile spring-boot:run -Dspring-boot.run.arguments="--server.port=8081"
```

**验证启动成功：**
```
Started MediaBackendApplication in X.XXX seconds
Tomcat started on port 8081
```

#### 3. 启动Community后端服务（端口8084）
```bash
# 新开一个终端窗口
cd community-backend
./mvnw clean compile spring-boot:run -Dspring-boot.run.arguments="--server.port=8084"
```

**验证启动成功：**
```
Started CommunityBackendApplication in X.XXX seconds
Tomcat started on port 8084
```

#### 4. 启动前端服务（端口5173）
```bash
# 新开一个终端窗口
cd frontend
npm install  # 首次运行需要安装依赖
npm run dev
```

**验证启动成功：**
```
VITE v5.4.21 ready in XXX ms
➜ Local: http://localhost:5173/
```

### 方法二：使用启动脚本（推荐熟练用户）

#### Windows系统
**文件**: `start-all.bat` (已提供)
```bash
# 双击运行或在命令行执行
start-all.bat
```

#### Linux/Mac系统
**文件**: `start-all.sh` (已提供)
```bash
chmod +x start-all.sh
./start-all.sh
```

## 🔍 验证服务状态

### 检查端口占用
```bash
# Windows
netstat -ano | findstr ":808"

# Linux/Mac
lsof -i :8081,8082,8084,5173
```

### API测试
使用浏览器或Postman测试以下API：

#### PetCare API
```bash
http://localhost:8082/api/pets
```

#### Media API
```bash
http://localhost:8081/api/v1/media/test
```

#### Community API
```bash
http://localhost:8084/api/v1/moments/user/76
```

#### 前端页面
```bash
http://localhost:5173
```

## 🐛 常见问题解决

### 1. 端口被占用
**错误**: `Port 8082 was already in use`

**解决方法**:
```bash
# Windows: 查找并杀掉占用端口的进程
netstat -ano | findstr :8082
taskkill /PID [进程ID] /F

# Linux/Mac:
lsof -ti:8082 | xargs kill -9
```

### 2. 数据库连接失败
**错误**: `Could not create connection to database server`

**解决方法**:
- 检查MySQL服务是否启动
- 验证数据库用户名密码
- 确认数据库名称正确

### 3. Maven编译失败
**错误**: `Failed to execute goal org.apache.maven.plugins`

**解决方法**:
```bash
# 清理Maven缓存
mvn clean

# 重新编译
mvn compile

# 如果还有问题，删除.m2文件夹重试
```

### 4. Node.js依赖安装失败
**错误**: `npm ERR! code ERESOLVE`

**解决方法**:
```bash
# 清理npm缓存
npm cache clean --force

# 删除node_modules和package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 5. Java版本不兼容
**错误**: `Unsupported class file major version`

**解决方法**:
- 确保使用Java 21
- 检查JAVA_HOME环境变量
```bash
java -version
echo $JAVA_HOME
```

## 📞 技术支持

如果遇到其他问题，请：

1. **检查日志**: 查看详细的错误信息
2. **查看文档**: 参考 `后端改动与问题记录.md`
3. **联系团队**: 在项目群中询问具体错误信息

### 常用调试命令
```bash
# 查看Java进程
jps -l

# 查看系统资源
htop  # Linux/Mac
tasklist  # Windows

# 测试数据库连接
mysql -h localhost -u username -p petcare_ecosystem
```

---

**祝您开发愉快！** 🎉