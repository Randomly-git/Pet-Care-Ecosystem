# 宠物护理生态系统 — 功能性测试用例表 & 静态测试指南

> **文档说明**：本文档包含两部分内容——（一）功能性测试用例表，基于系统功能性需求与现有代码实现；（二）前后端静态测试方法与操作指南。  
> **用例编号格式**：`T1.n.a`（T1=功能性测试，n=微服务编号，a=用例序号）。  
> **代码分析日期**：2026-05-18

---

# 第一部分：功能性测试用例表

---

## 表1：宠物日常记录微服务（R1）

> 对应后端模块：`petcare-backend`  
> 核心 Controller：`ActivityController`、`StatusController`、`PetController`

| 用例编号 | 用例名 | 用例说明 | 前置条件 | 输入 | 预测结果 | 测试结果 | 失败原因 |
|---|---|---|---|---|---|---|---|
| T1.1.1 | 创建带附件的活动记录 | 用户通过 `POST /api/activities/records/pet/{petId}` 提交一条带图片/视频文件的活动记录，系统保存记录数据并调用媒体服务上传文件，前端立即得到响应 | 用户已登录且有有效 JWT Token；宠物已创建 | petId=1, activityId=1, description="喂食", file=cat_food.jpg, userId=1 | HTTP 200，返回 ActivityRecord 对象（含 mediaId 关联），媒体文件异步上传至腾讯云 COS | 媒体文件成功上传 | 无 |
| T1.1.2 | 创建纯文字活动记录（无附件） | 用户仅提交文字描述的活动记录，不上传文件，系统仅保存文本数据 | 用户已登录；宠物已创建 | petId=1, activityId=2, description="散步30分钟", userId=1, file=null | HTTP 200，返回 ActivityRecord 对象，无媒体关联 | 活动创建成功 | 无 |
| T1.1.3 | 按条件分页查看活动记录（含附件加载） | 用户通过 `GET /api/activities/records/pet/{petId}` 查询宠物历史活动记录，支持时间范围与种类筛选，返回记录时加载关联媒体文件列表 | 用户已登录；宠物下已有至少一条活动记录 | petId=1, startDate=2026-01-01, endDate=2026-05-31, page=0, size=10 | HTTP 200，返回分页 ActivityRecordDTO 列表，每条记录包含 mediaFiles 数组；若媒体服务不可用，mediaFiles 返回空数组 | 成功查看分页活动 | 无 |
| T1.1.4 | 查看记录时媒体服务不可用（降级处理） | 模拟媒体服务宕机，用户查看活动记录时系统仍返回文字数据，mediaFiles 为空数组，不报 500 错误 | 媒体服务不可用；宠物有至少一条带附件的记录 | petId=1, startDate=..., endDate=..., page=0, size=10 | HTTP 200，返回记录列表，mediaFiles 为空数组，日志输出 WARN 信息 | **未能成功创建记录** | 尚未实现降级处理逻辑 |
| T1.1.5 | 更新活动记录触发定时提醒刷新 | 用户更新活动记录后，系统通过 RabbitMQ 发送事件，提醒服务监听到事件后自动更新对应定时活动的下一次提醒日期 | RabbitMQ 可用；该记录关联了定时活动 | recordId=1, PUT /api/activities/records/{recordId}，更新 description | HTTP 200，记录更新成功；MQ 消息已发送；定时活动的 reminder_date 按规则自动推算 | 成功刷新定时提醒 | 无 |
| T1.1.6 | 将日常记录分享至宠物空间 | 用户在日常记录页面点击"分享到空间"，系统向社区后端发起同步请求，在空间内创建一条新帖子 | 用户已登录；该记录属于当前用户；社区后端可用 | recordId=1, userId=1 | HTTP 200，社区空间新增一条帖子，内容包含原记录摘要 | **未能新增帖子** | `MomentController` 缺少从活动记录发布帖子的直连接口 |
| T1.1.7 | 删除活动记录并同步清理媒体文件 | 用户删除活动记录时，系统通过 MQ 异步通知媒体服务清除关联文件，记录本身立即删除 | 记录存在且有关联媒体文件；RabbitMQ 可用 | DELETE /api/activities/records/{recordId} | HTTP 200，记录已删除，MQ 删除事件已发布，媒体文件异步清除 | 记录与附件异步删除 | 无 |

---

## 表2：宠物空间微服务（R2）

> 对应后端模块：`community-backend`  
> 核心 Controller：`MomentController`、`CommentController`、`LikeController`、`FollowController`

| 用例编号 | 用例名 | 用例说明 | 前置条件 | 输入 | 预测结果 | 测试结果 | 失败原因 |
|---|---|---|---|---|---|---|---|
| T1.2.1 | 发布纯文字帖子 | 用户填写标题与内容后确认发布，系统将纯文本帖子保存至数据库，返回帖子详情 | 用户已登录且有有效 Token | POST /api/v1/moments, body: {userId, content, mediaIds: []} | HTTP 201，返回 MomentResponseDTO（含帖子ID、内容、auditStatus=PENDING），管理端待审核 | 发布成功 | 无 |
| T1.2.2 | 发布带附件的帖子（含媒体关联） | 用户先上传媒体文件获取 mediaIds，发布帖子时传入 mediaIds，系统通过媒体服务建立关联 | 媒体文件已上传至媒体服务；用户已知 mediaIds | POST /api/v1/moments, body: {userId, content, mediaIds: [101, 102]} | HTTP 201，返回 MomentResponseDTO，mediaFiles 包含已关联的 2 个文件信息 | 发布成功 | 无 |
| T1.2.3 | 浏览帖子列表（全平台，按时间倒序） | 用户进入宠物空间首页，系统返回审核通过的帖子列表，支持分页和上下滑动加载 | 平台中至少有一条已审核通过的帖子 | GET /api/v1/moments/all?page=0&size=10 | HTTP 200，返回帖子列表，按 createdAt 倒序排列，支持上拉加载更多（分页滚动） | 加载成功 | 无 |
| T1.2.4 | 点击帖子查看详情（浏览次数+1） | 用户点击某条帖子进入详情页，系统返回帖子全量内容（文字+媒体），浏览次数自动增加 1 | 帖子已存在且已审核通过 | GET /api/v1/moments/{momentId}?userId=1 | HTTP 200，返回 MomentResponseDTO，包含 mediaFiles 列表、作者信息、点赞数、评论数；该帖 viewCount +1 | 返回完整帖子信息 | 无 |
| T1.2.5 | 对帖子进行点赞/取消点赞（切换） | 用户对帖子点击点赞按钮，系统切换点赞状态，首次点赞成功，再次点击取消点赞 | 用户已登录；帖子存在 | POST /api/v1/likes, body: {userId, targetType: "MOMENT", targetId} | HTTP 200，返回"点赞成功"或"取消点赞成功"，likeCount 相应变化 | 操作与反馈匹配 | 无 |
| T1.2.6 | 发表评论与二级回复 | 用户对帖子发表一级评论，或对其他评论发表二级回复 | 用户已登录；帖子存在 | POST /api/v1/comments, body: {momentId, userId, content, parentCommentId: null/xxx} | HTTP 201，返回 CommentResponseDTO，若含 parentCommentId 则为回复 | 能发表二级回复 | 无 |
| T1.2.7 | 关注/取消关注其他用户 | 用户对其他宠物主发起关注，系统切换关注状态 | 用户已登录；目标用户存在 | POST /api/v1/follows, body: {followerId, followedId} | HTTP 200，返回"关注成功"或"取消关注成功"；followCount 相应变化 | 操作与反馈匹配 | 无 |

---

## 表3：帖子推荐微服务（R3）

> 说明：当前代码库中**未发现独立的帖子推荐微服务实现**。`community-backend` 提供了基础帖子查询能力，`statistics-backend` 提供了活动统计数据，但缺少根据宠物记录生成推荐关键词、搜索匹配帖子的推荐引擎。以下用例基于需求规格编写，预计部分用例将因功能缺失而失败。

| 功能模块 | 用例名 | 用例编号 | 用例说明 | 前置条件 | 输入 | 预测结果 | 测试结果 | 失败原因 |
|---|---|---|---|---|---|---|---|---|
| 帖子推荐微服务 | 基于宠物健康记录推荐相关帖子 | T1.3.1 | 推荐服务向后端请求某宠物的健康与活动数据，自动提取关键词（如"过敏""疫苗"），然后向社区服务搜索包含该关键词的帖子并返回 | 宠物有健康记录数据；社区中有相关帖子；推荐服务已部署 | petId=1（该宠物有"过敏"相关状态记录） | HTTP 200，返回与"过敏""护理"等关键词匹配的帖子列表，按相关度排序 | 待执行 | **未发现独立的推荐服务模块**：`statistics-backend` 仅提供活动统计，`community-backend` 仅提供基本 CRUD；缺少关键词提取、匹配搜索、排序推荐的核心逻辑 |
| 帖子推荐微服务 | 基于用户输入关键词搜索帖子 | T1.3.2 | 用户在搜索框输入关键词，系统向社区服务请求匹配的帖子列表，同时记录该关键词用于推荐优化 | 社区中有至少一条帖子内容包含该关键词 | keyword="疫苗" | HTTP 200，返回包含"疫苗"的帖子列表；关键词记录已保存用于后续优化 | 待执行 | **关键词搜索与记录功能未实现**：`community-backend` 缺少全文搜索或关键词匹配的 API；`statistics-backend` 无关键词存储逻辑 |

---

## 表4：媒体文件微服务（R4）

> 对应后端模块：`media-backend`  
> 核心 Controller：`MediaController`  
> 核心依赖：腾讯云 COS（`com.qcloud:cos_api`）、RabbitMQ

| 功能模块 | 用例名 | 用例编号 | 用例说明 | 前置条件 | 输入 | 预测结果 | 测试结果 | 失败原因 |
|---|---|---|---|---|---|---|---|---|
| 媒体文件微服务 | 上传图片并关联到业务实体 | T1.4.1 | 用户通过表单上传一张图片，媒体服务接收后上传至腾讯云 COS，在数据库中建立记录并关联到指定业务（如 MOMENT/ACTIVITY） | 腾讯云 COS 配置正确且可访问；数据库连接正常 | POST /api/media/upload, form-data: file=test.jpg, userId=1, relatedType=MOMENT, relatedId=10 | HTTP 200，返回 MediaResponse（含 mediaId、fileUrl、fileType 等），COS 上存在该文件，数据库已写入 | 待执行 | 无；`CosConfig` 已配置，`CosStorageService` 已实现 |
| 媒体文件微服务 | 上传视频文件 | T1.4.2 | 用户上传 MP4 视频文件，媒体服务正确处理视频类型并存储 | COS 可用 | POST /api/media/upload, form-data: file=test.mp4, userId=1, relatedType=ACTIVITY, relatedId=20 | HTTP 200，返回 MediaResponse，fileType="video/mp4" | 待执行 | 无 |
| 媒体文件微服务 | 查询某业务实体下的所有媒体文件 | T1.4.3 | 前端根据业务类型和 ID 请求获取关联的媒体列表，系统从数据库查询并返回文件元数据及临时访问链接 | 该业务实体下至少有一条媒体记录 | GET /api/media/related/MOMENT/10 | HTTP 200，返回该帖子下所有媒体文件的列表（含 fileUrl、fileType、fileSize） | 待执行 | 无 |
| 媒体文件微服务 | 删除单个媒体文件（数据库+COS） | T1.4.4 | 业务端发起删除请求，媒体服务从 COS 和数据库中同步删除指定文件 | 文件存在；COS 可访问 | DELETE /api/media/{mediaId} | HTTP 200，数据库记录已删除，COS 上对应文件已清除 | 待执行 | 无；`MediaService.deleteMediaFile()` 已实现同步删除 |
| 媒体文件微服务 | 批量删除某业务下的所有关联媒体 | T1.4.5 | 当帖子或活动记录被删除时，系统调用批量删除接口清除所有关联媒体 | 该业务下有多个媒体文件 | DELETE /api/media/related/MOMENT/10 | HTTP 200，所有关联文件已从数据库和 COS 中清除 | 待执行 | 无 |
| 媒体文件微服务 | 上传空文件（异常场景） | T1.4.6 | 用户提交空文件，系统返回明确的错误提示 | 无 | POST /api/media/upload, file=空文件 | HTTP 400 或 500，返回错误信息"文件不能为空" | 待执行 | 无；`MediaService.uploadMediaFile()` 有空文件校验 |
| 媒体文件微服务 | 批量将孤儿媒体文件绑定到具体业务 | T1.4.7 | 先上传媒体但暂不关联，后续通过批量接口绑定到指定业务实体 | 存在未关联的媒体文件（relatedId 为临时值） | PATCH /api/media/batch/related, body: {mediaIds:[1,2], relatedType:MOMENT, newRelatedId:30} | HTTP 200，返回更新数量，所有指定媒体已重新关联 | 待执行 | 无 |

---

## 表5：活动提醒微服务（R5）

> 对应后端模块：`petcare-backend`（`ReminderController`、`FixedActivityController`、`ReservedActivityController`）  
> 说明：定时活动提醒与日常记录微服务共享同一后端模块，通过 RabbitMQ 进行事件驱动交互。

| 功能模块 | 用例名 | 用例编号 | 用例说明 | 前置条件 | 输入 | 预测结果 | 测试结果 | 失败原因 |
|---|---|---|---|---|---|---|---|---|
| 活动提醒微服务 | 创建周期性定时活动（Fixed Activity） | T1.5.1 | 用户创建一项周期性活动（如每3天喂食），系统计算首次提醒日期并保存 | 用户已登录；宠物已创建 | POST /api/fixed-activities, body: {petId, activityId, gapDays:3, startDate:2026-05-18} | HTTP 201，返回 FixedActivity 对象，含计算的 reminder_date | 待执行 | 无 |
| 活动提醒微服务 | 创建单次未来活动预定（Reserved Activity） | T1.5.2 | 用户预定未来某天的单次活动（如预约兽医），系统创建一次性提醒 | 用户已登录；宠物已创建 | POST /api/reserved-activities, body: {petId, activityId, reminderDate:2026-06-01} | HTTP 200，返回 ActivityReminder 对象，type=2（单次），reminderDate=2026-06-01 | 待执行 | 无 |
| 活动提醒微服务 | 活动记录上传后自动刷新定时提醒日期 | T1.5.3 | 用户完成了一次定时活动打卡后，系统通过 MQ 事件驱动自动更新该定时活动的下一次提醒日期 | RabbitMQ 可用；该活动为定时活动；用户刚提交了一条活动记录 | 通过 POST /api/activities/records/pet/{petId} 提交打卡，触发 MQ 事件 | MQ 事件已发送；`ActivityReminder` 的 reminder_date 自动更新为 lastDate + gapDays | 待执行 | **需确认 MQ 消费者是否已实现**：`petcare-backend` 中有 `AiResultListener` 但未发现监听活动记录并更新提醒的消费者；当前 `FixedActivityService` 可能依赖手动或定时任务更新 |
| 活动提醒微服务 | 查看宠物过期/待办提醒列表 | T1.5.4 | 用户进入提醒页面，系统查询该宠物所有未完成且 reminder_date ≤ 今天的提醒并返回 | 宠物下至少有一个过期提醒 | GET /api/reminders/pet/{petId}/overdue | HTTP 200，返回 ReminderDTO 列表（包含活动名称、提醒日期、类型） | 待执行 | 无；`ReminderServiceImpl.getOverdueRemindersByPetId()` 已实现 |
| 活动提醒微服务 | 手动确认完成提醒（无描述） | T1.5.5 | 用户在列表中点击"完成"，系统将提醒标记为已完成，若是单次活动（type=2）则删除提醒 | 提醒存在且未过期 | PUT /api/reminders/{activityReminderId}/confirm, body: "" (空) | HTTP 200，记录已转为活动记录（无描述），若是 type=2 则提醒已删除 | 待执行 | 无 |
| 活动提醒微服务 | 手动确认完成提醒（含描述） | T1.5.6 | 用户完成提醒时附带文字描述（如"今天喂了牛肉罐头"），系统将该描述一并写入活动记录 | 提醒存在 | PUT /api/reminders/{activityReminderId}/confirm, body: "今天喂了牛肉罐头" | HTTP 200，活动记录已创建且含描述字段 | 待执行 | 无 |
| 活动提醒微服务 | 暂缓提醒（推迟一天） | T1.5.7 | 用户点击"稍后提醒"，系统将提醒日期推迟一天 | 提醒存在 | PUT /api/reminders/{activityReminderId}/postpone | HTTP 200，reminder_date 增加了 1 天 | 待执行 | 无 |
| 活动提醒微服务 | 修改周期性活动的间隔天数 | T1.5.8 | 用户调整定时活动频率（如从每3天改为每5天），系统更新 gapDays 并重新计算下次提醒日期 | 定时活动存在 | PUT /api/fixed-activities/{fixedActivityId}, body: {gapDays:5} | HTTP 200，FixedActivity 已更新，下次提醒日期已重新计算 | 待执行 | 无 |

---

## 表6：大模型微服务（R6）

> 对应后端模块：`llm-backend`  
> 核心组件：`PetHealthResolver`（GraphQL Query Resolver）  
> AI 服务：阿里云通义千问（Qwen）API

| 功能模块 | 用例名 | 用例编号 | 用例说明 | 前置条件 | 输入 | 预测结果 | 测试结果 | 失败原因 |
|---|---|---|---|---|---|---|---|---|
| 大模型微服务 | 宠物健康智能分析报告生成（GraphQL） | T1.6.1 | 前端发起 GraphQL 查询请求，系统多线程并行获取宠物基本信息与历史健康状态记录，构建结构化 Prompt 调用通义千问 API，生成定制化健康养护报告并返回 | petcare-backend 可用；通义千问 API Key 有效且额度充足；宠物有历史状态记录 | GraphQL query: `{ petHealthAnalysis(petId: "1") { petId name breed healthAdvice statusRecords { statusName description startDate } } }` | HTTP 200，返回 PetHealthData（含宠物名、品种、AI 生成的 healthAdvice 文本、状态记录列表） | 待执行 | **高风险 — 硬编码 API Key**：`PetHealthResolver` 中硬编码了通义千问 API Key `sk-972a2985...`（已暴露在源码中），存在安全隐患且可能已失效/超额；此外 `petcare-backend` 需通过 Nacos 服务发现可访问 |
| 大模型微服务 | 带用户自定义需求的健康分析 | T1.6.2 | 用户在查询中附加特定需求（如"重点关注皮肤问题"），系统将用户需求融入 Prompt 发送给 AI | 同 T1.6.1 | GraphQL query: `{ petHealthAnalysis(petId: "1", userRequirement: "重点关注皮肤问题") { ... } }` | HTTP 200，healthAdvice 内容中侧重皮肤护理建议 | 待执行 | 同 T1.6.1（API Key 风险） |
| 大模型微服务 | 宠物不存在时的错误处理 | T1.6.3 | 查询一个不存在的宠物 ID，系统能优雅返回错误信息而不崩溃 | petcare-backend 可用 | GraphQL query: `{ petHealthAnalysis(petId: "99999") { petId healthAdvice } }` | HTTP 200，返回 PetHealthData 对象但 healthAdvice 包含错误提示信息（如"宠物不存在"） | 待执行 | 无；`PetHealthResolver` 有 try-catch 兜底，调用 `createErrorResponse()` |
| 大模型微服务 | petcare-backend 不可用时的降级处理 | T1.6.4 | 模拟日常记录服务宕机，大模型服务应能捕获超时/连接异常并返回错误信息 | petcare-backend 不可用 | 同 T1.6.1 | HTTP 200（GraphQL 不抛 HTTP 500），返回错误 PetHealthData，healthAdvice 含"调用失败"等描述 | 待执行 | 无；`PetHealthResolver.getPetInfo()` 调用 RestTemplate，超时后会抛异常被外层 catch 捕获 |

---

## 附录：微服务模块映射总览

| 微服务编号 | 微服务名称 | 对应后端模块 | 主要 Controller |
|---|---|---|---|
| R1 | 宠物日常记录微服务 | `petcare-backend` | `ActivityController`, `StatusController`, `PetController`, `AuthController` |
| R2 | 宠物空间微服务 | `community-backend` | `MomentController`, `CommentController`, `LikeController`, `FollowController`, `NotificationController` |
| R3 | 帖子推荐微服务 | **未实现** | 无独立模块（`statistics-backend` 提供统计，但无推荐引擎） |
| R4 | 媒体文件微服务 | `media-backend` | `MediaController` |
| R5 | 活动提醒微服务 | `petcare-backend`（同 R1 模块） | `ReminderController`, `FixedActivityController`, `ReservedActivityController` |
| R6 | 大模型微服务 | `llm-backend` | `PetHealthResolver`（GraphQL） |

> **总结**：共 6 张表、**33 条**功能性测试用例。其中 **4 条**用例根据当前代码分析预测可能失败：  
> - **T1.1.6**（分享记录到空间）：缺少独立的分享 API 端点  
> - **T1.3.1、T1.3.2**（帖子推荐/关键词搜索）：推荐微服务模块整体未实现  
> - **T1.5.3**（打卡后自动刷新提醒）：MQ 消费者逻辑需进一步确认  
> - **T1.6.1、T1.6.2**（大模型健康分析）：硬编码 API Key 存在安全隐患且可能已失效

---

# 第二部分：静态测试方法与操作指南

## 一、后端静态测试

### 1.1 静态测试方法描述

后端静态测试是指**在不运行代码的情况下**，通过自动化工具对 Java 源代码或编译后的字节码进行扫描分析，检测潜在的代码缺陷、安全漏洞、性能问题和代码风格违规。

由于本项目是 **Java + Maven** 多模块工程（`petcare-backend`、`community-backend`、`media-backend`、`llm-backend`、`statistics-backend`、`petcare-gateway`），静态测试可以覆盖以下维度：

| 维度 | 检测内容 | 示例 |
|------|----------|------|
| **Bug 模式** | 空指针解引用、无限递归、错误比较（`==` vs `equals`） | `ActivityServiceImpl` 中 `file.getBytes()` 未做 null 检查 |
| **安全漏洞** | 硬编码密码/密钥、SQL 注入、路径遍历 | `PetHealthResolver` 中硬编码 `QWEN_API_KEY` |
| **性能问题** | 字符串循环拼接、冗余对象创建、未关闭的资源 | `for` 循环中 `+` 拼接字符串 |
| **代码坏味道** | 过长方法、过大类、重复代码、无用导入 | `ActivityController` 类过长 |
| **依赖问题** | 已知 CVE 漏洞的依赖版本、版本冲突 | `mysql-connector-j:8.3.0` 是否有 CVE |

**选用的静态分析工具：SpotBugs + PMD（Maven 插件方式）**

> **说明**：老师示例中使用的 **Detekt** 是 Kotlin 的静态分析工具。由于本项目是 **Java** 项目，我们选用 Java 生态中最成熟的等价工具组合：
> - **SpotBugs**（原 FindBugs）：字节码级 Bug 模式检测 —— 对应 Detekt 的 `potential-bugs` / `exceptions` 规则集
> - **PMD**：源代码级代码质量分析 —— 对应 Detekt 的 `code-smell` / `style` 规则集

### 1.2 安装与配置步骤

#### Step 1：在根 `pom.xml` 中添加 SpotBugs + PMD 插件

在项目根目录的 `pom.xml`（`e:\Documents\GitHub\Pet-Care-Ecosystem\pom.xml`）的 `<build><plugins>` 中添加：

```xml
<!-- SpotBugs 静态分析插件 -->
<plugin>
    <groupId>com.github.spotbugs</groupId>
    <artifactId>spotbugs-maven-plugin</artifactId>
    <version>4.8.6.0</version>
    <configuration>
        <effort>Max</effort>
        <threshold>Low</threshold>
        <xmlOutput>true</xmlOutput>
        <htmlOutput>true</htmlOutput>
        <outputDirectory>${project.build.directory}/spotbugs</outputDirectory>
    </configuration>
</plugin>

<!-- PMD 静态分析插件 -->
<plugin>
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-pmd-plugin</artifactId>
    <version>3.24.0</version>
    <configuration>
        <rulesets>
            <ruleset>/category/java/bestpractices.xml</ruleset>
            <ruleset>/category/java/codestyle.xml</ruleset>
            <ruleset>/category/java/errorprone.xml</ruleset>
            <ruleset>/category/java/security.xml</ruleset>
        </rulesets>
        <linkXRef>false</linkXRef>
        <outputDirectory>${project.build.directory}/pmd</outputDirectory>
    </configuration>
</plugin>
```

#### Step 2：运行静态分析

在 VS Code 终端中，切换到项目根目录，对**某一模块**（推荐 `community-backend` 或 `petcare-backend`）执行：

```powershell
# 进入目标模块目录
cd e:\Documents\GitHub\Pet-Care-Ecosystem\community-backend

# 执行 SpotBugs 静态分析
.\mvnw.cmd spotbugs:check

# 执行 PMD 静态分析
.\mvnw.cmd pmd:check
```

#### Step 3：查看分析报告

报告生成在模块的 `target/` 目录下：
- **SpotBugs HTML 报告**：`community-backend/target/spotbugs/spotbugs.html`
- **PMD HTML 报告**：`community-backend/target/pmd/pmd.html`

在 VS Code 中右键报告文件 → **Open Preview** 即可查看。

### 1.3 预期可检测到的问题示例（截图素材）

基于代码审查，以下问题预计会被 SpotBugs 或 PMD 检测到：

| 模块 | 文件 | 预计检测到的问题 | 严重级别 |
|------|------|------------------|----------|
| `llm-backend` | `PetHealthResolver.java:38` | **硬编码 API Key** — `private static final String QWEN_API_KEY = "sk-972a29..."` | 🔴 High |
| `petcare-backend` | `ActivityController.java` | **过长类** — 控制器包含过多方法（>200行） | 🟡 Medium |
| `community-backend` | `CommentController.java:44` | **System.err 直接输出** — 异常处理中使用 `System.err.println()` 而非日志框架 | 🟡 Medium |
| `community-backend` | `MomentController.java:45` | **CrossOrigin 通配符 `*`** — 允许任意来源跨域访问 | 🟡 Medium |
| `media-backend` | `MediaController.java:51` | **文件上传缺少大小限制校验** — `MultipartFile` 未约束大小 | 🟡 Medium |
| `petcare-backend` | `ReminderServiceImpl.java:46` | **RuntimeException 泛化抛出** — `throw new RuntimeException(...)` 应使用自定义异常 | 🟢 Low |

### 1.4 截图清单

执行后端静态测试后，请截取以下画面：
1. **终端截图**：运行 `mvnw spotbugs:check` 或 `mvnw pmd:check` 的输出（含警告数量和类别统计）
2. **SpotBugs HTML 报告截图**：打开的 `spotbugs.html` 报告页面（含 Bug 分类饼图和详情列表）
3. **PMD HTML 报告截图**：打开的 `pmd.html` 报告页面（含违规列表和代码定位）
4. **IDE 内问题高亮截图**（可选）：若在 VS Code 中安装了 **SonarLint** 扩展，可截图展示代码中直接标红/标黄的问题行

| 严重级别     | 数量  | 含义                                                         |
| ------------ | ----- | ------------------------------------------------------------ |
| 🔴 **High**   | **1** | `DM_DEFAULT_ENCODING` — `CommunityHBaseColdStorageService.java:128` 使用 [String.getBytes()](vscode-file://vscode-app/e:/Microsoft VS Code/0958016b2a/resources/app/out/vs/code/electron-browser/workbench/workbench.html) 依赖系统默认编码，换操作系统可能乱码 |
| 🟡 **Medium** | 68    | `EI_EXPOSE_REP2` — Lombok [@RequiredArgsConstructor](vscode-file://vscode-app/e:/Microsoft VS Code/0958016b2a/resources/app/out/vs/code/electron-browser/workbench/workbench.html) 将外部可变对象（如 Repository）直接存入字段，SpotBugs 认为暴露了内部表示 |
| 🟢 **Low**    | 31    | `REC_CATCH_EXCEPTION`（捕获未抛出的异常）、`SIC_INNER_SHOULD_BE_STATIC_ANON`（匿名类应改为静态内部类）、`UPM_UNCALLED_PRIVATE_METHOD`（私有方法未被调用/死代码） |

---

## 二、前端静态测试

### 2.1 静态测试方法描述

前端静态测试分为两个层面：

**层面一：ESLint 代码静态分析**  
对 Vue 3 单文件组件（`.vue`）的 `<template>`、`<script>`、`<style>` 三个区块分别进行规则检查，检测：未使用变量、`v-for` 缺少 `:key`、组件 props 未定义类型、潜在 XSS 漏洞（`v-html` 使用）等。

**层面二：JSX/模板结构树 + 单组件调试**  
利用 Vue DevTools 浏览器扩展，在不修改代码的情况下：

- 查看 **组件结构树（Component Tree）** —— 完整展示当前页面的组件嵌套层级关系
- 检查每个组件的 **Props、Data、Computed、Events** 状态
- 定位数据流异常（如 props 未传递、事件未触发）

### 2.2 安装与配置步骤

#### Step 1：安装 ESLint + Vue 插件

在 `frontend` 目录下执行：

```powershell
cd e:\Documents\GitHub\Pet-Care-Ecosystem\frontend
npm install --save-dev eslint eslint-plugin-vue @vue/eslint-config-standard
```

#### Step 2：创建 ESLint 配置文件

在 `frontend` 目录下创建 `.eslintrc.cjs`：

```javascript
module.exports = {
  root: true,
  env: { browser: true, es2022: true, node: true },
  extends: [
    'plugin:vue/vue3-recommended',   // Vue 3 推荐规则集
    '@vue/eslint-config-standard'
  ],
  parserOptions: { ecmaVersion: 'latest', sourceType: 'module' },
  rules: {
    'vue/multi-word-component-names': 'warn',
    'vue/no-unused-vars': 'warn',
    'vue/require-v-for-key': 'error',    // v-for 必须有 :key（防御性）
    'vue/no-v-html': 'warn',             // v-html 有 XSS 风险
    'vue/component-definition-name-casing': ['error', 'PascalCase']
  }
}
```

#### Step 3：添加 lint 脚本

在 `frontend/package.json` 的 `scripts` 中添加：

```json
"lint": "eslint src/**/*.{js,vue} --ext .js,.vue"
```

#### Step 4：运行静态分析

```powershell
cd e:\Documents\GitHub\Pet-Care-Ecosystem\frontend
npm run lint
```

#### Step 5：Vue DevTools 组件调试

1. 在 Chrome/Edge 浏览器中安装 **Vue.js devtools** 扩展
2. 启动前端开发服务器：
   ```powershell
   cd e:\Documents\GitHub\Pet-Care-Ecosystem\frontend
   npm run dev
   ```
3. 打开浏览器 `http://localhost:5173`，按 `F12` → 切换到 **Vue** 标签页
4. 在 **Components** 面板中查看组件树结构
5. 点击任意组件查看其 Props / Data / Computed 状态

### 2.3 预期可检测到的问题示例（截图素材）

| 文件 | 预计检测到的问题 | ESLint 规则 | 严重级别 |
|------|------------------|-------------|----------|
| `MomentsView.vue` | `v-for="m in moments"` 缺少 `:key` 绑定 | `vue/require-v-for-key` | 🔴 Error |
| `MomentCard.vue` | 使用了 `v-html` 渲染用户内容（XSS 风险） | `vue/no-v-html` | 🟡 Warning |
| `CreateMoment.vue` | props 未声明类型（`defineProps` 缺少类型约束） | `vue/require-prop-types` | 🟡 Warning |
| `ActivityRecords.vue` | 导入但未使用的变量/组件 | `vue/no-unused-vars` | 🟢 Warning |
| `App.vue` | 组件名不符合 PascalCase | `vue/component-definition-name-casing` | 🟢 Warning |

### 2.4 组件结构树预期展示

使用 Vue DevTools 查看 **Components** 面板时，以 `MomentsView` 页面为例，预期可见如下层级：

```
App.vue
└── AppLayout
    ├── AppHeader
    │   ├── Logo
    │   ├── NavMenu
    │   └── UserAvatar
    ├── MomentsView (当前页面)
    │   ├── ProfileCard (左侧栏)
    │   ├── GridControls (排版切换)
    │   ├── CreateMoment (发帖编辑器)
    │   │   ├── TextEditor
    │   │   └── MediaUploader
    │   └── MomentList (帖子列表)
    │       └── MomentCard × N (每条帖子)
    │           ├── UserInfo
    │           ├── ContentBlock
    │           ├── MediaGallery
    │           └── CommentItem (递归嵌套)
    ├── FollowSidebar (右侧栏)
    └── AppFooter
```

### 2.5 截图清单

执行前端静态测试后，请截取以下画面：
1. **终端截图**：运行 `npm run lint` 的输出（含 ESLint 错误/警告数量和具体文件行号）

   ![image-20260518223158060](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260518223158060.png)

2. **ESLint 问题代码截图**：在 VS Code 中打开有问题的 `.vue` 文件，截取 ESLint 红色波浪线标注的问题代码行

3. **Vue DevTools 组件树截图**：浏览器 F12 → Vue 标签页 → Components 面板，展示完整的组件嵌套树结构

4. **单组件调试截图**：在 Vue DevTools 中点击选中 `MomentCard` 组件，右侧面板展示该组件的 props（如 `moment` 对象）、data、computed 属性值

---

## 三、快速上手指令汇总

```powershell
# ========== 后端静态测试 ==========
cd e:\Documents\GitHub\Pet-Care-Ecosystem\community-backend

# SpotBugs（需先在根 pom.xml 添加插件配置）
..\mvnw spotbugs:check
# 报告位置：target/spotbugs/spotbugs.html

# PMD（需先在根 pom.xml 添加插件配置）
..\mvnw pmd:check
# 报告位置：target/pmd/pmd.html

# ========== 前端静态测试 ==========
cd e:\Documents\GitHub\Pet-Care-Ecosystem\frontend

# 安装 ESLint（仅首次）
npm install --save-dev eslint eslint-plugin-vue @vue/eslint-config-standard

# 创建 .eslintrc.cjs 配置文件（见上方配置内容）

# 运行 ESLint
npm run lint

# 启动开发服务器（配合 Vue DevTools 使用）
npm run dev
```
