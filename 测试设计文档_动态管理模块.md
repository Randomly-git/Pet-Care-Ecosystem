# 动态管理模块测试设计文档

## 7. 测试用例设计以及实现

### 7.1 测试用例设计

动态管理模块（`3.1.x` 系列需求）是 PetCare 社交系统的核心业务模块，承载动态创建、查询、修改、删除全生命周期，包含 PENDING/APPROVED/REJECTED/MIGRATING/COLD 五种状态及复杂的状态转移逻辑。

**需求覆盖清单**：共覆盖 25 个核心需求（3.1.1~3.1.25）：

| 需求分类 | 需求编号 | 需求描述 |
| --- | --- | --- |
| **创建** | 3.1.1~3.1.4 | 动态创建、媒体关联、异常处理、审核前不可见 |
| **查询** | 3.1.5~3.1.8 | 详情查询、状态判断、冷数据恢复、管理员操作 |
| **列表** | 3.1.9~3.1.17 | 用户列表、全站分页、HBase 冷数据扫描 |
| **修改** | 3.1.18~3.1.21 | 作者修改、权限验证、迁移状态保护 |
| **删除** | 3.1.22~3.1.25 | 级联删除、权限验证、冷数据删除 |

**输入字段约束**：

| 字段 | 类型 | 约束条件 | 覆盖技术 |
| --- | --- | --- | --- |
| userId | integer | min=1, nullable=True | EP, BVA |
| content | string | maxLength=2040, nullable=True | EP, BVA |
| mediaIds | integer | min=1, nullable=True | EP, BVA |
| momentId | integer | min=1, nullable=False | EP, BVA |
| page | integer | min=0, default=0 | EP, BVA |
| size | integer | min=1, max=100, default=20 | EP, BVA |

**测试技术矩阵**：

| 技术 | 缩写 | 适用场景 | 覆盖目标 |
| --- | --- | --- | --- |
| **等价类划分** | EP | 输入参数的有效/无效划分 | 边界外值、空值、非法值 |
| **边界值分析** | BVA | 数值型输入的边界验证 | 边界点、边界内外值 |
| **状态转移测试** | STT | 有限状态机的状态转换 | 所有合法/非法状态转换 |

**状态转移覆盖（STT）**：基于 STT.csv，筛选出 reqid 包含 3.1.x 的状态转移路径共 10 条，覆盖从 PENDING→APPROVED→REJECTED→MIGRATING→COLD→TERMINATED 的全生命周期。

### 7.2 测试工具实现

#### 7.2.1 测试用例说明（Test Case Specification）

测试过程的起点是测试用例表格，它定义了每条测试的具体输入数据、执行条件和预期结果。本项目的测试工具基于 JUnit 5 (Jupiter) 参数化测试框架构建，辅以 Spring RestTemplate 作为 HTTP 客户端。

选择理由如下：项目本身基于 Spring Boot 3.2.0 构建，spring-boot-starter-test 已默认集成 JUnit 5、AssertJ、Mockito 等完整测试栈，无需额外引入依赖，避免版本冲突风险。被测接口为 RESTful API，RestTemplate 提供了 exchange()、postForEntity() 等原生方法，配合 ResponseEntity 可完整获取状态码、响应头和响应体，满足黑盒接口测试的全部需求。JUnit 5 的 @ParameterizedTest + @MethodSource 机制天然适合数据驱动的测试场景——测试逻辑与测试数据完全分离，新增用例只需编辑 CSV 而无需改动代码，可维护性极强。

作为替代方案，REST Assured 的 BDD 风格链式调用在可读性上优于 RestTemplate，但引入它会增加项目依赖，且与本项目已使用的 RestTemplate 模式不统一，权衡后不予采用。同样地，TestNG 虽支持 @DataProvider 做数据驱动，但 JUnit 5 已是 Spring Boot 官方默认，统一性更佳。

本项目覆盖社区动态模块的 25 个功能需求（需求 3.1.1~3.1.25），采用等价类划分（EP）和边界值分析（BVA）两种黑盒测试技术。每条用例以一行 CSV 记录表示，包含测试用例 ID、需求 ID、测试技术、标题、测试数据字典（如 `{'userId': 1, 'content': 'aaa', 'mediaIds': 1}`）、覆盖分区、预期结果描述和有效/无效标志位。此外，针对 7 类状态转换场景（如 PENDING→APPROVED、MIGRATING→COLD），额外设计了 STT（State Transition Testing）用例集，其 CSV 采用 15 列结构，包含源状态、事件、守卫条件、目标状态、动作、是否禁止等字段。

将这两份 CSV 文件作为初始输入提供给 AI，要求其基于 JUnit 5 参数化测试框架生成测试代码。AI 的第一版输出包含一个完整的 MomentsApiTest.java，以 @ParameterizedTest + @MethodSource 驱动 CSV 数据，按需求 ID 将用例路由到对应的 HTTP 方法（POST 创建、GET 查询、PUT 修改、DELETE 删除），并通过 isValid 标志位控制断言方向。

#### 7.2.2 测试过程说明（Test Procedure Specification）

AI 为每种 HTTP 方法分别封装了执行方法：executeCreate() 调用 restTemplate.postForEntity() 完成 POST 请求，executeGetMoment() 调用 exchange(url, GET) 完成 GET 查询，testUpdateMoment() 调用 exchange(url, PUT) 完成修改，testDeleteMoment() 调用 exchange(url, DELETE) 完成删除。

每个方法均遵循相同的三层异常处理策略：

```
try {
    ResponseEntity<String> resp = restTemplate.exchange(...);
    // 正常响应的状态码验证
    assertTrue(status == 200 || status == 404, ...);
} catch (HttpClientErrorException e) {
    // 4xx 异常：提取状态码继续验证而非直接 fail
    int status = e.getStatusCode().value();
    assertTrue(status == 404, ...);
} catch (HttpServerErrorException e) {
    // 5xx 异常：对因环境因素导致的服务器错误允许测试继续执行
    int status = e.getStatusCode().value();
    assertTrue(status == 500, ...);
} catch (Exception e) {
    // 其他异常：有效用例则报错，无效用例则通过
    if (isValid) fail(...);
}
```

测试过程与用例数据完全分离——新增或修改用例只需编辑 CSV，测试代码无需变更，这也是数据驱动框架的核心优势。

**首次迭代**：首次运行测试即遭遇大面积失败，约 135 条用例报错。分析后发现，问题并非服务端缺陷，而是测试过程对 Spring RestTemplate 的异常处理机制考虑不周。具体来说，executeGetMoment() 中原本的 catch (Exception e) { fail(e.getMessage()) } 逻辑过于宽泛：RestTemplate 在收到 4xx 响应时会抛出 HttpClientErrorException，该异常被 catch 块捕获后直接触发 fail()，根本不会执行到原先设想的 status == 200 断言。将此根因反馈给 AI 后，AI 在每个执行方法中插入了 HttpClientErrorException 的独立捕获分支，使测试过程正确处理 4xx 响应。

**第二轮迭代**：失败数从 135 降至约 78。进一步分析日志发现，一批 BVA 有效用例（如 userId=1、mediaIds=1）返回了 400。检查请求体后发现，这些用例的 CSV 行中未包含 content 字段，而 buildBody() 方法将其默认设为空字串 ""，触发了服务端的内容非空校验。AI 据此将 buildBody() 中 content 的默认值从 "" 改为 "a"，使这些用例通过请求校验阶段。

**第三轮迭代**：观察到约 50 个 500 错误。其中 Update 接口的 500 源于 momentId 不存在时服务端未返回 404 而是抛出了未处理异常，导致 HttpServerErrorException 触发 fail。AI 为 testUpdateMoment() 新增了 HttpServerErrorException 的独立捕获分支，将 500 视为可接受的响应。而 Create 接口的 500 则指向更深层的问题——mediaService.validateMediaIds() 依赖的 media-backend 服务未启动，属于环境问题而非测试可修复的范畴，因此保持为已知失败。

#### 7.2.3 测试事件报告与测试日志（Test Incident Report & Test Log）

Test Incident Report 记录实际结果与预期结果不符时生成的缺陷事件。在上述四轮迭代中，每次运行测试后都将 console 全部输出（即《测试日志》）提供给 AI，AI 据此统计通过/失败数量、分析失败根因、定位到具体代码位置（精确到行号）。例如，从日志中观察到 executeGetMoment() 第 616 行的 fail() 被触发后，复现了异常抛出路径并确认了根因。每次迭代的失败分类记录在表 7.3.2 中，按服务端校验缺失和环境依赖缺失两类汇总。

### 7.3 测试结果分析

执行全部 419 条测试用例（385 条 EP/BVA + 34 条 STT）后，测试通过率约为 88%。

**表 7.3.1 测试结果总表**

| 指标 | 数据 |
| --- | --- |
| 用例总数 | 419 |
| 通过 | 369 |
| 失败 | 50 |
| 通过率 | 88% |

在已通过的 369 条用例中，有三类成果值得关注：其一，所有 GET 查询详情接口全部通过。momentId 不存在时服务端返回 404，测试框架的 HttpClientErrorException 捕获逻辑将其判为有效响应，使原本因异常抛出而误判为失败的 60 余条用例全部通过。其二，所有无效参数校验场景均通过——userId 为 null、非整数、content 超长、mediaIds 非法等边界值均被服务端正确拒绝，返回 400。其三，原 STT 场景的 17 条 405 路由错误已在测试代码中修复（新增 parseCsvLineStt() 解析器和 testSttScenarios() 路由分发逻辑），不再计入当前失败。

失败的 50 条用例归因于两类问题。

**表 7.3.2 失败原因分类表**

| 分类 | 失败数 | 占比 | 涉及接口 | 根因 |
| --- | --- | --- | --- | --- |
| 服务端校验缺失 | 30 | 60% | 查询用户列表（3.1.9）、全站分页（3.1.13）、删除（3.1.22~3.1.25） | Controller 层缺少 @Valid 等 JSR-380 校验注解 |
| 环境依赖缺失 | 20 | 40% | 创建动态（3.1.1~3.1.4） | media-backend 服务未启动，Feign 调用超时 |

**服务端校验缺失（30 条）**：查询用户列表（3.1.9）中，userId=0、page=-1、page=null、page=非整数、size=0、size=101、size=null、size=非整数等 12 个无效参数应返回 4xx，但服务端实际返回 200 与空结果。全站分页（3.1.13）中 page=-1 和 size=101 同样未做校验，共 2 条。删除接口（3.1.22~3.1.25）的 momentId=0 和 userId=0 也表现出相同问题，共 16 条。根因在于 Controller 层缺少 @Valid、@Min、@Max 等 JSR-380 校验注解。修复建议：在分页参数 DTO 中添加 @Min(0) int page 和 @Min(1) @Max(100) int size，并在 Controller 方法参数前加 @Valid。

**环境依赖缺失（20 条）**：创建动态接口（3.1.1~3.1.4）的有效用例全部返回 500，响应体为 [no body]。审查服务端日志发现，MomentController.createMoment() 内部调用 mediaService.validateMediaIds() 时，由于 media-backend 服务未启动，Feign 调用超时导致事务回滚。修复建议：在测试环境中启动 media-backend 服务，或在测试配置文件中注入 Mock MediaService Bean 以屏蔽外部依赖。

